import { QueryClient } from "react-query";

export const queryClient = new QueryClient();

/*
 * React Query
 * https://react-query.tanstack.com/reference/QueryClient#queryclientgetdefaultoptions
 */
